# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules

# Build output
dist
dist-ssr

# Environment files
.env
.env.local
.env.*.local
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel

# Per-worktree ownership marker (npm run claim) — local only
.worktree-claim.json

# deno check writes this next to the function it checks; not a project file
deno.lock

# Frankie eval runs — reports with real answers; local only
scripts/frankie-eval/runs/

# one-off geocode backfill outputs
scripts/geocode-misses.json
scripts/geocode-job-misses.json
