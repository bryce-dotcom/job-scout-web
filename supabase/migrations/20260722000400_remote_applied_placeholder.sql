-- Placeholder. This migration was applied to the remote from another
-- worktree/branch; the file is recorded here so the CLI's history check
-- passes. Intentionally a no-op — the real DDL already ran.
select 1;
